{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the trusted setup window only. The remote app window (label 'app') is intentionally absent, so remote content gets no IPC.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-close",
    "dialog:default"
  ]
}
