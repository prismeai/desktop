{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the trusted local setup window ('main').",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
